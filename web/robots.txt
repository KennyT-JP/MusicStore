# 音源創庫 / Track Cabinet
#
# **このファイルが無いと、クローラーは読み物に辿り着けない。**
#
# 2026-08-15 の AdSense 審査で「パブリッシャーのコンテンツを含まない画面に
# おける広告」の指摘が 2 回続いた。原因は広告の位置ではなく、**発見経路**
# だった：
#
#   - 広告は読み物（/help/{ja,en}/*.html・本文 1,600 字以上）にだけ置いてある
#   - ところが `/robots.txt` も `/sitemap.xml` も存在せず、rewrite の
#     `**` → /index.html に飲まれて**アプリの HTML が返っていた**
#   - トップ（/）は CanvasKit なので読める文字が「読み込んでいます…」だけ
#   - そのトップから読み物への `<a>` が 1 本も無かった
#
# つまり**審査側から見えるのは中身の無いページ 1 枚だけ**で、14 枚の読み物は
# 存在しないのと同じだった。sitemap を置けば、リンクを辿らなくても
# 読み物へ直接来られる（共有ドキュメント `ナレッジベース.md` S-7）。
#
# **URL は本番のものを書く。** 検証環境へ配信するときは
# `scripts/deploy.mjs` が配信物の側だけを検証環境のドメインに差し替える
# （原本は触らない）。検証環境の sitemap が本番を指していると、
# 検証環境をクロールした結果が本番の評価に混ざる。

User-agent: *
Allow: /

Sitemap: https://music-storage-d79b2.web.app/sitemap.xml
