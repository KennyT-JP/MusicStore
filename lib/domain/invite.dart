/// 招待 URL の受諾（仕様書 3.3 / 13.3）
///
/// **判定そのものはサーバー側にある**（functions/src/domain/invite.ts）。
///
/// 以前はここに同じ規則の写しがあり、13 件のテストで手厚く守られていた。
/// ところが**本番コードからは一度も呼ばれていなかった**。実際に動いて
/// いたのは Cloud Functions のインライン実装で、そちらは無テストだった
/// （監査 S11・第2回）。**テストがあることと、守られていることは別。**
///
/// 写しを消し、判定は動いている側（サーバー）へ集約した。
/// ここに残すのは、画面が文言を出し分けるための種別だけ。
library;

/// 招待を受諾できない理由。
enum InviteRejection {
  /// 招待そのものが存在しない（URL が誤っている）。
  notFound,

  /// 有効期限を過ぎている（3.3）。
  expired,

  /// すでに使用済み（ワンタイム／3.3）。
  alreadyUsed,

  /// 発行者が取り消した。
  revoked,

  /// すでにそのリストのメンバーである。
  alreadyMember,
}
