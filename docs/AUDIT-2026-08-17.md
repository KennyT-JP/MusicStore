# ゼロベース監査 第 5 回（2026-08-17）

**手順書**: `C:\Codes\SessionConcierge\ゼロベース監査手順_共有版.md`
**開始点**: `403fa25`（`main`・作業ツリー clean）
**体制**: **7 観点を独立・並行**で実施（観点 7「アンチパターン照合」は今回が初回）。
各担当には §0（原則）＋自分の観点の節だけを渡し、他観点の結論も過去の監査結果も渡していない。

---

## 実測（観点 6 を第 5 回の基準値とする）

自動テスト **1,318 件**（flutter 747 / functions 単体 154 / 統合 215 / ルール 202）。すべて緑。

| 項目 | 値 | 取得 |
| --- | --- | --- |
| `build/web/main.dart.js`（生） | 4,200,956 B | `ls -la build/web/main.dart.js` |
| 本番 `main.dart.js`（br） | 881,442 B | `curl -H "Accept-Encoding: br" -w '%{size_download}'` |
| 初回表示の合計転送（Chrome 概算） | 約 3.8 MB | 上記の合算 |
| lib の `snapshots(` | 20 | grep |
| lib の上限なしコレクション取得 | 12 | grep＋1 件ずつ確認 |
| 索引（複合/override） | 3 / 4（全 7 件がクエリと対応） | `firestore.indexes.json` |

**前回値の台帳が無い**（数字が開発記録に散在）。次回はこの表のコマンドで再測定して並べる。

---

## 突き合わせ（最大の収穫）

### 複数の観点が同じ箇所を指した → 根が 1 つ

**「実装が仕様書とマニュアルを追い越した」。** 今日 1 日でモバイル対応・ダウンロード・
プレミアム基盤が入り、コードとテストは正しいのに文書だけが数日前の姿で残った。

| 箇所 | 指した観点 | 実装 | 文書 |
| --- | --- | --- | --- |
| ファイル差し替え | 3・5 | ✅ 8/14 | 仕様書 6.3「未実装」／マニュアル「まだできません」（**本番配信中**） |
| プレミアム／クーポン | 2・3・5 | ✅ | 仕様書 §15「まだ着手しない」／マニュアル記載なし |
| モバイル／DL | 2・3・5・6 | ✅ | 仕様書「将来」／マニュアル記載なし |
| 孤児ファイル保持の UI | 3 | ✅ | 仕様書 13.3「画面に無い」 |
| データモデル | 3 | ✅ | 仕様書 13.2 図に `coupons`・`private/state` なし |

### 評価が食い違った箇所 → 無し
観点 4・7 がともに「quota 既定値 Dart 1GB / TS 2GB」を見て、独立に「別概念（表示用/強制用）で重複ではない」と結論。矛盾なし。

### どの観点も触れなかった領域 → 無し
**A3 で差分ゼロを確認**（監査中に入った変更なし）。各担当がネイティブ層・配信物を全量リストに含めたため、前回の落とし穴 1（リストに載らない層）も塞がれた。

---

## 指摘一覧と対応

### 群 A（文書と実装の食い違い）— **2026-08-17 に修正済み**
仕様書 §6.3/6.4/13.2/13.3/13.5/§15/§3.1/§12.7、マニュアル原本（差し替え・Apple・プレミアム・DL）、`DOWNLOAD-DESIGN.md` 冒頭、`BACKLOG.md` プレミアム節。**「未実装／未着手／将来」を実装済みに。**

### 群 D（軽微）— **2026-08-17 に修正済み**
`index.ts` ヘッダ表の callable 3 本追加、テスト件数 959→1,318（取得方法併記）、コメント参照先誤り（`download_sync.dart` の `204-212`→`282`）。

### 群 B（セキュリティ・性能）— **未対応・依頼者判断待ち**
| # | 指摘 | 観点 | 程度 |
| --- | --- | --- | --- |
| S1 | `redeemCoupon` にレート制限が無い（短い指定コードは総当たり可） | 1 | 中 |
| P1 | DL 同期の起点が永久に前進しない（`lastSyncedAt` 未保存 → 全件読みへ漸近） | 6 | 中 |
| P2 | 未読件数の購読が全件を流す＋通知の定期削除が無い | 6 | 中 |
| — | セッション寿命の購読が画面を離れても残る（`allLists`・`myListRequests` ほか） | 6 | 低 |

### 群 C（再発防止の空白）— **未対応・依頼者判断待ち**
| AP | 指摘 | 観点 |
| --- | --- | --- |
| AP-71 | **成果物の中身検査が自動化されていない**（今日 42.4MB の空 APK を踏んだ地点） | 7 |
| AP-12 | **Web ログインのポップアップ待ちに上限が無い**（逃げ道なし） | 7 |
| AP-76/46 | 並行ビルドの排他・容量単位変更時の影響者計数が手順依存（強制なし） | 7 |

---

## 総括

- **セキュリティ・権限・列挙遮断・冪等性・ネイティブ層は堅牢**。重大（読める/なりすませる）な穴は無し
- **観点 7（初回）が確認**：AP 78 件中 60 件が機械的な見張りあり。この製品は AP 番号を直接引用して回帰を固定しており、成熟度が高い
- **弱点は 2 か所に集中**：①文書が実装を追い越された（群 A・修正済み）②再発防止の空白がモバイル配信パイプラインと外部待ちのタイムアウトに残る（群 C・未対応）
