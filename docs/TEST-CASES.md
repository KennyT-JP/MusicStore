# 手動テストケース台帳（仕様書 12.6）

**自動テストでは守れないものだけを載せます。**

いま自動テストは 1,318 件あり（`scripts/check.mjs`。2026-08-16 実測。
内訳は flutter 747／functions 単体 154／統合 215／ルール 202）、権限判定・
セキュリティルール・容量・連番・共有リンク・画面の組み立ては、そちらが見ています。
**同じものをここに書き写すと、数だけ増えて何も守りません。**

載せるのは次のどれかに当たるものだけです。

- **人の目や耳が要る**（音が鳴る・見た目が崩れていない）
- **実物のメールや外部サービスが要る**（確認メール・Google 連携）
- **配信先でしか起きない**（キャッシュ・索引・クラウド側の権限）
- **時間がかかる**（30 日後の自動削除）

---

## 使い方

1. **検証環境**で行います（仕様書 12.2）。本番では行いません。
2. 実施したら、その行の「最終実施」を `YYYY-MM-DD` に書き換えます。
   まだ一度もやっていないものは `未実施` のままにします。
3. 期待と違ったら、**その場で直さずに記録**してください。何が起きたかを
   残すほうが、直した記憶より確かです。

> **この台帳は `test/domain/manual_test_cases_test.dart` が見張っています。**
> ID の重複、空欄、実在しない仕様の節・テストファイルへの参照があると
> 落ちます。**書き写した注意書きではなく、数えられる形にするため**です
> （[AUDIT-CHECKLIST.md](AUDIT-CHECKLIST.md)「注意書きを、仕組みと数えない」）。

---

## 台帳

| ID | 対象 | 事前条件 | 手順 | 期待結果 | 自動テストとの関係 | 最終実施 |
| --- | --- | --- | --- | --- | --- | --- |
| M-01 | 14.1 | スマートフォン、または幅 390px のブラウザ | サイト管理 → ユーザー管理を開く | 名前とメールアドレスが**横に伸びる**。1 文字ずつ縦に折り返さない | `test/ui/admin_screens_test.dart` が幅を測るが、**実機の描画は見ていない** | 2026-08-11 |
| M-02 | 8.1 | 音源が 1 件以上あるリスト | 一覧の ▶ を押す | **実際に音が聞こえる**。2 曲目を押すと 1 曲目が止まる | `test/ui/playback_ui_test.dart` は操作の対象を見るだけ。**音は判定できない** | 2026-08-11 |
| M-03 | 6.1 / 7.2 | Super User 以上、容量に空きがあるリスト | 実ファイルを選んで曲を追加する | 追加でき、**使用容量が増える**。連番が 1 つ進む | 統合テストは Firestore の値のみ。**実ファイルの往復は通らない** | 未実施 |
| M-04 | 8.2 | 音源が 1 件以上あるリスト | 曲の詳細からダウンロードする | ファイルが落ち、**外部のアプリで開ける** | 無し。ブラウザと OS の受け渡しは自動化できない | 未実施 |
| M-05 | 3.1 | Google アカウント | ログイン画面で「Google で続ける」を押す | アカウントを選ぶとログインでき、**メール確認を求められない** | 無し。外部サービスの画面を通る | 2026-08-11 |
| M-06 | 3.1 | 受信できるメールアドレス | メールで登録し、届いた確認リンクを踏む | **確認メールが実際に届き**、踏むと先へ進める | 統合テストはエミュレータの確認済みフラグを立てるだけ。**実メールは飛ばない** | 未実施 |
| M-07 | 3.1 | 登録済みのメールアドレス | 「パスワードをお忘れですか？」から再設定する | **再設定メールが届き**、新しいパスワードでログインできる | 無し。同上 | 未実施 |
| M-08 | 3.3 | リスト管理者以上と、別のアカウント | 共有リンクを発行し、**別の人**が開いて「リストのメンバーになる」を選ぶ | メンバーになり、**曲やコメントを足せる**（Super User） | 統合テストが役割を確かめる。**別の人・別の端末で開く経路は通らない** | 未実施 |
| M-09 | 3.3 | 同上 | 同じリンクで「リストのメンバーにならずに見る」を選ぶ | 中身は見られるが、**書き込みの導線が出ない**。メンバー一覧にも出ない | 統合テストが viewers を確かめる。**画面の出し分けは実機で見る** | 未実施 |
| M-10 | 10.2 | メンバーが 2 人以上いるリスト | 片方が曲を追加する | もう片方に**ベルの件数が増え**、通知一覧に出る | 統合テストが通知の書き込みを確かめる。**届く様子は実機で見る** | 未実施 |
| M-11 | 3.4 | ログイン済み | 設定で English に切り替え、主な画面を一巡する | **日本語が残らない**。エラー表示も英語になる | `test/domain/l10n_usage_test.dart` は定義の使用を見るだけ。**画面の実物は見ていない** | 未実施 |
| M-12 | 12.2 | 直前に配信したこと | 再読み込みして、直した箇所を見る | **その場で新しい版が出る**（1 時間待たされない） | `test/domain/hosting_cache_test.dart` が設定を見る。**実際に届くかは配信先でしか分からない** | 2026-08-11 |
| M-13 | 14.2 | ログイン済み | 各画面の右上のヘルプを押す | **その画面の節**が別のタブで開く。表示言語に合った言語で出る | `test/domain/help_links_test.dart` が節の有無を見る。**実際に開くかは実機で見る** | 未実施 |
| M-14 | 12.5 | 検証環境 | 使い方のページで、章をまたぐリンク・目次へ戻る・言語切替を押す | どれも**同じ話題の場所へ着く**。言語を切り替えても読んでいたページのまま | `test/domain/help_links_test.dart` は節の実在を見るだけ。**押した先に着くかは実機で見る** | 未実施 |
| M-15 | 13.4 | 削除して 30 日以上たった曲があること | Storage の中身を見る | ファイルが**実際に消えている**。使用容量が減っている | 統合テストが `runPurgeNow`（定期実行と同じ中身）を通しで確かめる。**本物の時計で 30 日待つ経路は通らない** | 未実施 |
| M-16 | 11.1 | サイト管理者、削除してよい利用者 | サイト管理からユーザーを削除する | 曲と音源が消え、**コメントは「退会したユーザー」として残る** | 統合テストが通るが、**エミュレータは索引を強制しない**（2026-08-10 の欠陥はこれで見逃した） | 未実施 |
| M-17 | 3.5 | 投稿のあるアカウント | 設定から退会する | 投稿は残り、表示名だけ**「退会したユーザー」**になる | 統合テストが値を確かめる。**画面の見え方は実機で見る** | 未実施 |
| M-18 | 13.5 | Read Only のアカウント | 音源のダウンロードと、アップロードを試す | **ダウンロードはでき、アップロードはできない** | ルールテストがエミュレータで確かめる。**本番の Storage で違う可能性は残る** | 未実施 |
| M-19 | 12.5 | AdSense の審査に通ったあと | 使い方のページと、アプリの画面を順に開く | **使い方のページには広告が出て、アプリの画面には出ない** | `test/domain/ads_placement_test.dart` がコードの置き場所を見る。**実際に出るかは審査後にしか分からない** | 未実施 |
| M-20 | 12.5 | 配信した直後（本番・検証とも） | `curl` で `/robots.txt` `/sitemap.xml` を叩き、**中身まで**読む | robots.txt に `Sitemap:` 行があり、sitemap の `<loc>` が**その環境のドメイン**を指している | テストは `web/` の原本を見るだけ。**配信物に届いたか、rewrite に飲まれていないかは外から叩かないと分からない**（2026-08-15 に 2 回審査に落ちた原因）。2026-08-16 の実施結果：検証・本番とも 17 URL、読み物 16 枚が 200、ドメインの取り違え 0 件 | 2026-08-16 |
| M-21 | 12.5 | 本番へ配信した直後 | `curl -i` で `/.well-known/apple-app-site-association` を叩く | **200 で `Content-Type: application/json`** が返り、中身が JSON | `test/domain/app_links_test.dart` は `firebase.json` の記述を見るだけ。**配信エミュレータは headers を一切反映しない**ので、実効値は本番でしか測れない | 未実施 |
| M-22 | 12.4 | iOS 実機・アプリ導入済み | 曲の追加でファイル選択を開く | **落ちず、選択画面が出る** | **Session Concierge に `file_picker` の前例が無い。** Usage Description が要るかは実機でしか分からない（要ると分かったら Info.plist に足す） | 未実施 |
| M-23 | 3.5 | iOS / Android 実機・`GoogleService-Info.plist` 反映済み | Google でログインする | 認証後、**アプリの画面に戻る** | `CFBundleURLTypes` が `REPLACE_ME` のままだと、**認証は済むのにアプリへ戻らず「固まった」ように見える** | 未実施 |
| M-24 | 12.5 | 両ストア配信後・実機にインストール済み | 共有リンクを別アプリ（メール等）から開く | **ブラウザではなくアプリが開く** | `assetlinks.json` の SHA-256 が違うと**照合が黙って失敗し、ブラウザで開くだけ**になる。**照合はインストール時に一度だけ**なので、配信を先・インストールを後にする | 未実施 |
| M-25 | 3.5 | iOS 実機 | Sign in with Apple でログインする | ログインでき、表示名を決められる | developer.apple.com の App ID 側のチェックと**対で、片方だけでは動かない**。`Runner.entitlements` の記述だけでは足りない | 未実施 |
| M-26 | 12.4 | iOS 実機・ダウンロード済みの曲あり | Finder（または iTunes）で端末のバックアップを取り、中身を見る | `Library/Application Support/downloads` が**含まれない** | `test/domain/ios_backup_exclusion_test.dart` は Dart と Swift の**文字列の一致しか見ていない**。**Windows では Swift をビルドできない**ので、そもそもコンパイルが通るかも実機（またはクラウド CI）でしか分からない | 未実施 |
| M-27 | 12.4 | iOS / Android の実機それぞれ | 機内モードにして、ダウンロード済みの曲を再生する | **音が鳴る**。白リストの拡張子 6 種すべてで | **iOS は AVPlayer、Android は ExoPlayer で実装が別**なので、**片方は他方の証拠にならない**。`flac` と `ogg` は iOS で扱いが違い、**ブラウザで鳴ったことは証拠にならない** | 未実施 |

---

## 数えかた

- **全 27 件**。うち実施済みは「最終実施」に日付が入っているものです。
- **M-13 は 2026-08-13 に「未実施」へ戻しました。** マニュアルを章ごとの
  ページに分けて飛び先が変わったので、**前の確認は新しい飛び先を
  確かめたことになりません**（仕組みが変わったら、実施記録も戻す）。
- 自動テストの件数は `scripts/check.mjs` の出力が正本です。
  **この文書に件数を書き写さないでください**（片方だけ古くなります）。
