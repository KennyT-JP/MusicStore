# ゼロベース監査 第 6 回（2026-08-22）

**手順書**: `C:\Codes\SessionConcierge\ゼロベース監査手順_共有版.md`
**開始点**: `54d8fbe`（`dev`・作業ツリー clean）
**体制**: **7 観点を独立・並行**で実施（各担当に §0 原則＋自分の観点の節と `AUDIT-CHECKLIST.md` の該当項目のみを渡し、他観点の結論も過去の監査結果も渡していない）。
**A3**: 監査中に入った変更なし（HEAD は開始時と同じ `54d8fbe`、作業ツリー clean。担当は全員 read-only）。

---

## 実測（観点 6 を第 6 回の基準値とする）

自動テスト **1,409 件**（flutter 795 / functions 単体 187 / 統合 225 / ルール 202）。すべて緑。dart analyze 問題なし。前回（第 5 回）は 1,362 件。

| 項目 | 第 5 回 | 第 6 回 | 増減 | 取得 |
| --- | --- | --- | --- | --- |
| `build/web/main.dart.js`（生） | 4,200,956 B | 4,203,791 B | +0.07% | `ls -la` |
| 本番 `main.dart.js`（br） | 881,442 B | **測定不能** | — | `curl`（この環境は外向き通信が塞がれ 3 回とも接続タイムアウト。アプリの問題ではない。次回持ち越し） |
| lib の `snapshots(` | 20 | 20 | ±0 | grep |
| 上限なしコレクション取得 | 12 | 12（新規なし） | ±0 | grep＋1 件ずつ |
| 索引（複合/override） | 3 / 4 | 3 / 5 | override +1 | `firestore.indexes.json` |

override +1 は第 5 回 P2 の `notifications/createdAt`（COLLECTION_GROUP）が正規に入ったもの。劣化ではない。collectionGroup クエリ 7 本すべてに索引の宣言があり、未使用の索引宣言も無い（1:1 対応を全件照合）。

---

## 突き合わせ（§1.1・最大の収穫）

### 複数の観点が同じ箇所を指した → 根が 1 つ

**根 A：容量モデルが per-list→per-user へ作り直された残骸が、複数の面に残っている。**

| 面 | 指した観点 | 状態 |
| --- | --- | --- |
| 仕様書 7.2「リストごと 1GB」 | 3 | **旧モデルのまま**（15.1「人ごと 2GB・自動拡張」と内部矛盾。コードは 15.1 側で正しい） |
| per-list 容量 UI 2 か所（新規リスト既定・`setListQuota`） | 3 | **表示専用で効かない**のに入力欄が残る。`setListQuota` docstring に「表示専用」の明記なし |
| マニュアルの「使用容量／Storage used」 | 5 | 実ラベル「使用中の容量（…合計）」と不一致 |

今日のドキュメント同期は「サイト管理者＝実効プレミアム」に集中し、**容量モデルの旧記述（仕様 7.2）には及んでいなかった。**

**根 B：回帰防止の網（テスト・見張り）に、実装は正しいのに退行を検出できない穴がある。**

| 箇所 | 指した観点 | 穴 |
| --- | --- | --- |
| `notifications.createdAt` 索引ガード | 2 | ガードの `containsAll` に未追加。索引を消しても全 1,409 件が緑。本番でのみ purge がクエリ失敗（第 4 回 M-16 と同クラス） |
| `lib/domain/premium.dart`（`isActive`/`normalizeCouponCode`） | 4 | Dart 側にテスト 0 件（TS 側はあり）。境界取り違え・正規化破損を検知できない |
| 死蔵検査 helper（`_stripComments` 等） | 7 (AP-54) | 「見張りの見張り」不在。helper が退行しても緑のまま |
| purge の未読通知が limit 枠を占有 | 6 | 「未読が limit を占有する」経路の見張りが無い |

### 観点によって評価が食い違った → 実質なし
`ads.dart` を観点 3（docstring が旧 provider 名）と観点 4（別判定を作らず良好）が指したが、挙動は正しくコメントだけ古い＝別レイヤーで矛盾なし。容量の「自動拡張の管理者非適用（正しい）」と「per-list UI の残骸（問題）」も別レイヤー。

### どの観点も触れなかった領域 → なし
ネイティブ層・配信物は観点 1（Manifest/plist/assetlinks）・6（AdMob の Web stub）・7（AdMob ID 一致）がカバー。A3 で監査中の差分ゼロを確認。

---

## 指摘一覧と対応 — **2026-08-22 に修正済み（群 A・B1・C1〜C5）**

### 群 A：根が深い（複数観点が指した）— **中**

| # | 指摘 | 観点 | 箇所 | 対応 |
| --- | --- | --- | --- | --- |
| A1 | 容量モデルの三者不整合＋旧 UI 残骸 | 3・5 | 仕様書 7.2 / `site_admin_screens.dart:485,1832` / `setListQuota` docstring | ✅ 仕様 7.2 を「15.1 参照・per-list は表示専用」に改訂。per-list 容量 UI（新規リスト既定値・`setListQuota`）に「表示専用」を明記（撤去はせず注記に留めた）。`setListQuota` docstring も明記。`PREMIUM-DESIGN.md` に実装との差分を記録。C6（マニュアルの表記不一致）は今回は未着手（別途） |
| A2 | `notifications.createdAt` 索引ガード欠落 | 2 | `test/domain/download_sync_query_test.dart:196` | ✅ `containsAll` に追加。さらに B2 対応で `isRead+createdAt` 複合索引も追加し、そのガードテストも新設 |
| A3 | `lib/domain/premium.dart` に Dart 側テスト無し | 4 | `lib/domain/premium.dart:24,41` | ✅ `test/domain/premium_test.dart` 新設（19 件。TS 側と同じ境界・入力で固定） |

### 群 B：再発防止の空白 — **中〜低**

| # | 指摘 | 観点 | 箇所 | 対応 |
| --- | --- | --- | --- | --- |
| B1 | 死蔵検査 helper の「見張りの見張り」不在（AP-54） | 7 | `test/domain/no_dead_code_test.dart`, `l10n_usage_test.dart` | ✅ 両 helper（`_stripComments`／`_stripLineComments`＋`_isKeyUsed`）に既知の意地悪な入力（コメント混入・CRLF・URL・貪欲マッチ）を与える単体テストを追加（計 14 件）。**この過程で実際の不具合を 1 件発見・修正**：`l10n_usage_test.dart` の `_allSource()` がコメントを一切除去しておらず、ドキュメントコメント中の文字列だけで「使用あり」に数えられる状態だった（実害はまだ無かった） |
| B2 | purge の未読通知が limit 枠を占有し削除が滞りうる | 6 | `functions/src/scheduled/purge.ts:105-145` | ✅ クエリ段階で `isRead == true` に絞ってから `createdAt` の範囲を引く形に変更（合成索引を追加）。取得後の `shouldPurgeNotification` 判定は最終防衛線として残す |

### 群 C：軽微 — **低**

| # | 指摘 | 観点 | 箇所 | 対応 |
| --- | --- | --- | --- | --- |
| C1 | `siteAdminUids()` フォールバックが無効化管理者を除外しない | 1 | `functions/src/notifications.ts:203-209` | ✅ `access.ts` の `scanSiteAdmins`（`!user.disabled` 済み）を再利用する形に統一。走査の実装を 1 箇所に |
| C2 | `redeemCoupon` レート制限プリチェックが tx 外（並行バーストで一瞬緩む） | 1 | `functions/src/callable/coupons.ts:285` | ✅ ロック確認をトランザクションの中に移し、失敗数の増加と直列化 |
| C3 | 文書内のテスト件数が陳腐化（TEST-CASES.md は自ルール違反） | 2 | `docs/TEST-CASES.md:5`, `SETUP.md`, `BACKLOG.md` | ✅ 件数の書き写しを削除し「正本は `scripts/check.mjs` の出力」に統一 |
| C4 | `runPurgeNow` に本番 UI 導線が無い | 3 | `functions/src/callable/site_management.ts:319` | ✅ docstring から「即時削除」用途を削除。統合テスト用途のみに限定（仕様書 13.4 には元々記載なし） |
| C5 | `ads.dart` docstring が旧 provider 名（挙動は正しい） | 3 | `lib/config/ads.dart:104` | ✅ `isPremiumOrAdminProvider` に修正 |
| C6 | マニュアルの「使用容量／Storage used」が実ラベルと不一致 | 5 | `docs/manual/ja.html`, `en.html` | ⏸ **未対応**（今回のスコープ外。別途対応） |

### 是正作業中に見つかった副作用（是正・検証済み）

**A1 の是正がリグレッションを起こしていた。** `defaultQuotaLabel` という l10n キーが、表示専用の per-list 既定値入力（サイト設定画面）と、**実際に効く per-user の `setUserQuota` ダイアログ**の両方で共用されていた。A1 で「（表示専用）」を追記した際にこの共用に気づかず、**実際に効く方の入力欄まで「表示専用」とラベル付けしてしまった**。既存テスト `premium_screens_test.dart`「容量上限は『人ごとの合計』に効くと書いてある」がこれを検出（フルスイート実行で赤）。`perListDefaultQuotaLabel` という別キーを新設し、per-list 側だけをそちらへ切り替えて修正。日英とも対応、`flutter analyze`・関連テストで確認済み。

**functions 統合テストが並列実行時にポート 8080 タイムアウトで落ちた。** 単独実行では 225/225 成功、ポートの残留占有も無し。**5 本並列実行時のエミュレータ起動競合による一過性の失敗**と判断（AUDIT-CHECKLIST「時間制限を伸ばしても、接続が切れる事故は別枠」と同種）。コードの問題ではない。

## 最終検証（全是正を統合後）

`scripts/check.mjs`（5 本並列）— **全緑**。flutter 823 / functions 単体 187 / 統合 225/225 / ルール 202、dart analyze 問題なし。

### 情報（欠陥ではない・記録のみ）

- ダウンロード権限は**機密性の境界ではない**（`storage.rules:82` の read はメンバー/viewer に開く＝再生のため。仕様 3.3 と整合）。`verifyDownloadAccess` / `canDownload` が守るのはオフライン保存という製品ゲート。
- DL 同期の起点 `lastSyncedAt` 未永続化（前回 P1）。**第 5 回から悪化なし**。設計コメントが限界を明記済み。
- 役割表「再生・ダウンロード」と音源ブラウザ DL 終了予告の並存（マニュアル内のニュアンス。誤読防止の但し書きは任意）。

---

## 総括

- **重大・なりすまし・水増し・権限昇格の穴は無し**（観点 1・7）。今回入った「サイト管理者＝実効プレミアム／全リスト DL」は Dart/TS 両側で完全一致し、権限昇格の穴なし。容量自動拡張の管理者非適用も仕様・コードで整合。
- **性能劣化なし**（観点 6）。主要指標は横ばい、新規の購読・全件読み・Web 起動コストの追加なし（AdMob は Web=stub、SDK 初期化は遅延）。
- **弱点は 2 つの根に集中**：①容量モデルの作り直し残骸（仕様 7.2・per-list UI・マニュアル／群 A1・C6）②回帰防止の網の穴（索引ガード・Dart 側テスト・見張りの見張り・purge 未読／群 A2・A3・B1・B2）。**いずれも「動いているが、退行や誤設定を検出/防止できない」タイプで、通常のテストでは緑になる。**
- **群 A・B1・C1〜C5 は 2026-08-22 に是正・検証済み**（`scripts/check.mjs` 全緑）。是正の過程で新たな実害を 2 件検出・修正した——l10n キー共用による表示専用ラベルの誤爆（A1 の副作用）、`l10n_usage_test.dart` のコメント除去漏れ（B1 が見つけた実際の穴）。**監査の指摘を機械的に直すだけでなく、直す過程でテストが実際に効いた**好例。
- **未対応（別途）**: C6（マニュアルの実ラベル不一致）、B2 の根治的な設計変更は不要と判断（クエリ側で対処済み）。設計判断を伴う撤去（per-list 容量 UI そのものの削除）は行っていない。

## 次回への申し送り

- **本番 br 圧縮サイズを測れなかった**（外向き通信制限）。通信可能な環境で再測し、基準値台帳に並べる。
- **索引を追加したら、そのガードテスト（`download_sync_query_test.dart` の `containsAll`）も同時に更新する**——第 4 回 M-16 と同クラスの取りこぼしが第 6 回でも出た。索引とガードを対でコミットする規律を検討。
- **機能を per-X→per-Y へ作り直したら、旧 X の入力 UI・仕様節・マニュアル表記を全部数えて消す**（容量が per-list→per-user で 3 面に残骸を残した）。
