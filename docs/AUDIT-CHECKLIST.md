# ゼロベース監査 追加確認項目（本プロジェクト固有）

社内共有版の「ゼロベース監査 手順書」に**追加して**使ってください。
手順書の 6 観点はそのまま実施し、各観点でここに挙げた項目も見ます。

追加のきっかけは、**その回の監査で見つけられなかった欠陥**です。
見つけられた欠陥は手順書がすでに拾えているので、ここには載せません。

---

## 観点 1・3 共通：クライアントとサーバーで「同じ制約」に触れていないか

> **1 回目（2026-08-06）で最も痛かった見落とし。**
> サーバー側の誤りは指摘できたのに、**クライアント側のまったく同じ誤りは
> 「動いているはず」と仮定して素通りした**。結果、リストを作ってもホームに
> 出ない状態が配信後まで残った。

- [ ] 同じ種類の問い合わせが、クライアントとサーバーの両方にないか探す
- [ ] 片方で問題が見つかったら、**もう片方を必ず実際に動かして確かめる**
- [ ] 言語やライブラリが違っても、**基盤の制約は共通**であることを疑う

**やり方：** 片方の誤りを見つけたら、その API 名（今回なら `collectionGroup`
`documentId`）でリポジトリ全体を検索し、**ヒットした箇所を 1 件ずつ実行して確かめる。**
読んで「正しそう」で済ませない。

---

## 観点 1・3 共通：コメントとルールが食い違っていないか

> **2 回目に見つかった。** セキュリティルールに
> 「連番はトランザクション、容量は Functions が更新する」とコメントしながら、
> その下は `allow write: if false` だった。**項目の追加が一度も成功しない**
> 状態が配信後まで残った。

- [ ] ルールのコメントが説明している動作を、**そのルールが実際に許しているか**確かめる
- [ ] 「クライアントが書く」と書かれた項目に、書き込みの許可があるか
- [ ] 全面禁止（`if false`）の隣に、例外を述べたコメントが無いか

**やり方：** ルールのコメントは「意図」であって「実装」ではない。
両者が食い違っていても誰も気づかない。**コメントを読んだら、その動作を
テストで再現する。**

---

## 観点 2：「一度も成功していない経路」を洗い出す

> テストが無い経路は、**壊れたのではなく最初から動いていない**可能性がある。
> 「以前は動いていた」という前提を置かない。

- [ ] テストが 0 件の経路を列挙する
- [ ] そのうち**利用者が最初に通る経路**（ログイン直後の画面など）を優先して実行する
- [ ] 画面を開いて「空です」と出たとき、**本当に空なのか、取得に失敗しているのか**を切り分ける

**やり方：** 空の表示は「データが無い」と「取れなかった」の区別がつかない。
取得そのものを、実データを入れた状態で 1 度は動かす。

---

## 観点 2：ローカルで通るテストが、配信先でも通るとは限らない

- [ ] エミュレータと本番で**挙動が違う機能**を把握しているか
- [ ] 「エミュレータでは検証できない」と書かれた箇所は、**まず実際に試す**

> 1 回目では「Storage エミュレータは cross-service の参照に対応していない」という
> 前提で 8 件のテストを止めていた。**実際に外して動かしたら 12/13 が成功した。**
> しかもその誤った前提は 4 つの文書に転記されて増幅していた。
>
> **「〜できない」と書かれた箇所こそ、まず試すこと。**

---

## 観点 3：定義の数を「実際に効いている数」と読み替えない

- [ ] 文言・設定・索引などの**定義数**と、**実際に使われている数**を別々に数える
- [ ] 差があれば、その差の分だけ全量リストから漏れている

> 1 回目では、文言の定義数（123）を「画面に出る文字列の総数」と読んだ結果、
> **定義を通らない約 88 箇所が全量リストから丸ごと落ちた。**

---

## 観点 4：テストが守っているのは「本番で動いているコード」か

- [ ] テストが呼んでいる関数に、**本番からの呼び出し元があるか**を確かめる
- [ ] 無ければ、実際に動いている実装がどこかを探し、そちらのテストの有無を見る

> 1 回目では、連番と招待 URL のロジックがテストで手厚く守られていたが、
> **本番コードから一度も呼ばれていなかった。** 実際に動いていたのは
> 別の場所のインライン実装で、そちらは無テストだった。

---

## 観点 4：前提が崩れると自動的に通る項目が無いか

> **2 回目に見つかった。** 統合テストでエミュレータのプロジェクト ID が
> 噛み合っておらず 24 件が FAIL した。そのとき「本人には届かない」
> 「参加していない人には届かない」といった項目が **PASS** になっていた。
> 通知が 1 件も作られていないのだから、当然通る。
> **土台が崩れているほど緑が増える**という出方をしていた。

- [ ] 「〜が起きないこと」「〜が無いこと」を確かめる項目を数える
- [ ] その項目は、**前提が全部壊れていても通ってしまわないか**を考える
- [ ] 通ってしまうなら、前提そのものを確かめる項目が別にあるか

**やり方：** 否定を確かめる項目は、単独では意味を持たない。
「起きるはずの側」が同じ実行の中で確かめられていて初めて意味が出る。
前提の確認は最初にまとめて行い、崩れていればその場で止める。

---

## 観点 2：「動かせない」と書いた本人を、いちばん疑う

> **2 回目で最も痛かった。** 「この環境では統合テストを実行できない」と
> 書いたのは**監査の主宰者自身**だった。制約に従ったまま断定し、
> 開発ログに転記した。担当が実際に試したら動き、105/105・47/47 が通った。
>
> 1 回目の申し送りは「**『〜できない』と書かれた箇所こそ、まず試すこと**」。
> **前提を作る側が、その前提をいちばん疑いにくい。**

- [ ] 「〜できない」と書かれた箇所の**書いた人**を確かめる。自分なら、なおさら試す
- [ ] 根拠は書かれているか。試した手順は残っているか
- [ ] 制約は「今の設定では」なのか「原理的に」なのかを区別する

**やり方：** 「動かせない」と書くときは、必ず**試したコマンドと出力**を添える。
添えられないなら、それは推測であって事実ではない。

---

## 観点 2：常に赤いテストがあるなら、それ自体を欠陥として扱う

> **2 回目に見つかった。** 「この 3 件の赤は環境のせいです」と繰り返し
> 説明していた。対照実験の結果、**本物のセキュリティ後退が起きても
> 出力が変わらない**ことが分かった。
>
> | 条件 | 上書き禁止のテスト |
> | --- | --- |
> | ルールを壊した・前提が生きている | **失敗（検出できた）** |
> | ルールを壊した・前提が壊れている | **成功（見逃す）** |

- [ ] 常に失敗しているテストはあるか。その理由は何か
- [ ] その状態で、**本物の後退が混ざったら気づけるか**を考える
- [ ] 気づけないなら、前提が壊れていることを**先に検出して止める**

**やり方：** 赤に慣れることは、検出能力そのものを削る。
「環境のせい」で片づけず、前提の確認をテストの最初に置き、
崩れていればその場で理由を出して止める。

---

## 観点 4：指摘を記録しただけでは直らない

> **1 回目と 2 回目で同じ指摘が出た。** 「テストがあるのに本番から
> 呼ばれていない」コードは、1 回目の記録に残したにもかかわらず、
> **コードも README も直っていなかった**。

- [ ] 前回の記録にある指摘のうち、**まだ直っていないもの**を先に数える
- [ ] 直したものは、**再発しない形**になっているか（記録だけではないか）
- [ ] 機械的に確かめられる指摘は、テストに落とせないか

**やり方：** 「呼ばれているか」は数えられる。
`test/domain/no_dead_code_test.dart` のように、指摘そのものをテストにする。
**実際、このテストは監査でも挙がっていなかった死蔵を 1 件検出した。**

---

## 観点 1・6：直したことが、新しい欠陥を持ち込んでいないか

> **2 回目に見つかった。** メール確認をサーバー側で強制する修正
> （1 回目の S3）が、**新規登録そのものを壊していた**。
> 登録処理は「読んで、無ければ作る」順なのに、読みだけが禁じられた。
>
> 既存のテストは「未確認でも**作れる**」しか見ておらず、
> **読む側を一度も試していなかった。**

- [ ] 前回の修正箇所で、**対になる操作**が壊れていないか
      （書けるなら読めるか。読めるなら書けるか）
- [ ] 権限を厳しくした変更は、**その権限を必要とする手順の途中**を切っていないか
- [ ] 修正のテストは、片側だけを確かめていないか

**やり方：** 権限を 1 つ変えたら、その資源に対する操作を**すべて並べて**確かめる。
片方だけのテストは、片方だけの保証しかしない。

---

## 観点 5：目視でしか確かめられないと思っている項目を疑う

- [ ] 「見た目」「雰囲気」の要件が、**数値で確かめられないか**を検討する

> 配色を「寒色系で統一」と決めたが、これは色相という数値なので機械的に確かめられる。
> `test/ui/theme_test.dart` で固定してある。
> 目視に頼ると、配色を変えたときに戻ってしまっても気づけない。

---

## 観点 6：配信後にしか出ない問題を、配信前に潰す

- [ ] 既存データに**新しい項目**が要る変更は無いか（あるなら移行手段を用意する）
- [ ] 既存利用者が**使えなくなる**変更は無いか（認証の要件を厳しくした等）
- [ ] 新しく作られる関数・索引はあるか（初回作成でしか行われない設定に注意）

> 1 回目では、メンバーに `uid` 項目を足す変更と、認証にメール確認を必須にする変更が
> あった。前者は `scripts/backfill.mjs`、後者は手順書への注意書きで対応した。

---

## 観点 6：「名前が変わらない」を「中身が変わらない」と読み替えない

> **2 回の監査がどちらも見つけられなかった。** 配信の設定
> （`firebase.json`）でフォントを `immutable`（1 年、取り直さない）に
> していた。ところが Flutter はアイコン用の
> `MaterialIcons-Regular.otf` を、**そのビルドで使うアイコンだけに
> 削り込んで作り直す。** 名前は変わらないのに中身が変わるファイルである。
>
> 結果、**新しい画面が古いフォントで描かれ**、増やしたアイコンだけが
> 出なかった。ボタンは場所を取るが絵柄が無い、という出方をする。
> 強制リロードでも直らない。

- [ ] キャッシュを長く効かせている資産を列挙する
- [ ] そのうち、**ビルドのたびに作り直されるもの**が混ざっていないか
      （生成物・削り込まれるフォント・結合されたファイル）
- [ ] 「変わらない」と判断した根拠は、**ファイル名**か、**中身**か

**やり方：** 2 回続けて配信し、**同じ名前のファイルの中身が変わっているか**を
実際に比べる。名前だけを見て判断しない。
中身が変わりうるものに `immutable` を付けてはいけない。
`must-revalidate` なら、同じ内容のときは 304 が返るだけで転送量はほとんど変わらない。

**もう一つの教訓：** この不具合は**配信して初めて出る**。
ローカルでも、テストでも、ビルド成果物を調べても正常だった。
**「手元では動くのに配信先で動かない」ときは、アプリの外側**
（配信の設定・キャッシュ・ヘッダ）**を疑う層に入れること。**

---

## 観点 2・6：ビルドが成功しても、足したものが入っているとは限らない

> **2026-08-07。** `just_audio` を足して配信したが、**Web 版が組み込まれない
> ままビルドされていた**。前に作った生成物が残っていて、古い部品一覧が
> そのまま使われていた。
>
> ```
> MissingPluginException(No implementation found for method init
> on channel com.ryanheise.just_audio.methods)
> ```
>
> **どこにも赤が出ない。** ビルドは成功し、配信も成功し、画面も普通に開く。
> **その部品を使う操作だけ**が失敗する。
> `flutter test` も緑のままだった。テストは Dart のコードを直接動かすので、
> Web のビルドを通らないためである。

- [ ] この版で**新しく足した部品**を数える
- [ ] その部品を使う操作を、**配信先で 1 度は実際に動かす**
- [ ] 自動テストがその部品を通っているか。通っていないなら、何が守っているのか

**やり方：** 部品を足した版は、**手元のテストだけでは何も保証できない**と考える。
テストが通る経路と、配信先で動く経路が別だからである。
配信後に一度動かす手順を、リリースの手順に組み込む。

`scripts/deploy.mjs` は、部品の顔ぶれが前回のビルドから変わっていたら
自動で `flutter clean` する。ただし**これは事故を減らすだけで、
動いたことの確認にはならない。**

---

## 観点 6：症状の原因を、アプリの中だけで探さない

> **2026-08-07 に 3 件続けて起きた。どれもアプリのコードは正常だった。**
>
> | 症状 | 原因のあった場所 |
> | --- | --- |
> | 追加したアイコンが出ない | 配信の設定（キャッシュ指定） |
> | 呼び出しが `internal` で失敗 | クラウド側の権限（Cloud Run の呼び出し許可） |
> | 再生できない | ビルドの生成物（古い部品一覧） |
>
> **3 件とも `flutter test` は緑だった。**

- [ ] 「手元では動くのに配信先で動かない」に当てはまるか
- [ ] 当てはまるなら、**アプリの外側の層**を順に挙げる
      （ビルドの生成物・配信の設定・キャッシュ・クラウド側の権限）
- [ ] 層ごとに、**機械的に確かめる方法**があるか

**やり方：** 層を挙げてから、上から潰す。
今回効いたのは、生成されたフォントの中身を直接読む・
ビルドした成果物をヘッドレスブラウザで動かす、といった
**推測を挟まない確認**だった。スクリーンショットの目測は当たらない。

---

## 観点 2：例外を握りつぶすことは、欠陥そのものとして扱う

> **2026-08-07。** 再生の失敗を `catch (_)` で捨て、
> 「再生できませんでした」とだけ出していた。
> **原因が誰にも分からず、依頼者に何度も配信をやり直させた。**
>
> ```dart
> catch (_) { ...再生できませんでした... }
> ```
>
> 原因を出す作りにしていれば 1 往復で済んだ。

- [ ] `catch` で受けた値を**捨てている**箇所を数える
- [ ] 利用者に出すメッセージから、**原因が 1 つに絞れるか**
- [ ] 絞れないなら、詳細を読める導線があるか

**やり方：** 「失敗しました」とだけ出す作りは、**直す機会そのものを奪う。**
利用者向けの文言はやさしいままでよいが、
技術的な内容へ辿り着ける道を必ず残す。

---

## 観点 4：片側だけ塞ぐと、もう片側で同じことが起きる

> **第 3 回で分かった。** 第 2 回で「テストがあるのに本番から呼ばれて
> いない」コードを指摘し、Dart 側に見張り
> （`test/domain/no_dead_code_test.dart`）を置いた。
> **TypeScript 側には置かなかった。**
>
> 結果、第 3 回で **TypeScript 側の `canWrite` / `isMember` が
> 本番から 0 参照**という、まったく同じ欠陥が見つかった。
> しかもテストは 6 件あり、すべて緑だった。

- [ ] 見つけた欠陥に「同じ規則が当てはまる別の場所」が無いか数える
- [ ] このリポジトリは **Dart と TypeScript に同じ規則がある**。片方を
      直したら、もう片方を必ず見る
- [ ] 見張りを 1 つ置いたら、**その見張りが必要な場所を全部数える**

**やり方：** 対処のときに「ここだけか？」と一度立ち止まる。
片側だけの対処は、**直したという記録だけが残って、もう片側は育つ。**

---

## 観点 4：「〜すること」という注意書きを、仕組みと数えない

> **第 3 回で分かった。** サーバーの符号一覧をテスト側に写し、
> 「**増やしたら両方直すこと**」と注意書きを添えていた。
> 書いた本人以外は読まないし、読んでも忘れる。
> 守られなかったときに気づく手立ても無い。
>
> 直し方は簡単だった。**写すのをやめて、その場で読む。**

- [ ] コード中の「〜すること」「忘れずに」というコメントを数える
- [ ] それぞれ、守られなかったときに**気づける仕組みがあるか**
- [ ] 機械的に確かめられるなら、注意書きではなくテストにする

**やり方：** 同じ内容が 2 か所にあるなら、片方が正本で、もう片方は
**正本から読む**か、**一致を確かめるテストを置く**。
「両方直すこと」と書くのは、どちらでもない。

---

## 観点 1・6：直したときは、対になる側を見る（第 2 回と同じ指摘）

> **第 3 回でも出た。** 確認メールを「いま使っている言語」で送るよう
> 直したその日に、**登録した人の表示言語は `'ja'` 固定のまま**だった。
> メールは英語・画面は日本語という食い違いになった。
>
> この項目は第 2 回の申し送りにもある。**その申し送りを読んでいながら、
> 同じ形を作った。**

- [ ] 直した対象に「対になるもの」は無いか
      （送る／受け取る、書く／読む、メールの言語／画面の言語）
- [ ] 対になる側は、**同じ入口をすべて通っているか**
      （users を作る入口は 3 つあった。1 つ直して 3 つと思い込まない）

**やり方：** 直した値がどこから来てどこへ行くかを、**入口の数だけ**辿る。
「1 か所直した」で終えない。

---

## 観点 2：テストの中の「除外」が、環境によって効かなくなっていないか

> **2026-08-07。** リポジトリのファイルを走査するテストで、
> パスを `'lib/domain/'` のように `/` 区切りの文字列と比べていた。
> **Windows では `lib\domain\...` と円記号区切りで返るため、
> 除外が一切効かない。**
>
> 怖いのは失敗ではなく、**黙って別の意味になる**ことである。
>
> | テスト | Windows での実際の動き |
> | --- | --- |
> | 使われていない文言 | 生成物を除外できず、**常に通る**（何も守らない） |
> | 死蔵コード | 定義側を除外できず、**見逃す側に倒れる** |
>
> どちらも Linux では正しく動くので、書いた本人は緑を見て終わる。

- [ ] テストの中に「この範囲は除く」という判定はあるか
- [ ] その判定は、**実行する環境が変わっても同じ意味になるか**
      （パス区切り・改行・大文字小文字）
- [ ] 除外が効かなくなったとき、そのテストは**落ちるか、通るか**
      通るなら、それは前提が崩れると自動的に通る形である

**やり方：** 除外を書いたら、**除外が効かない場合を 1 度作って**
テストが落ちることを確かめる。パスは `test/support/repo_files.dart` の
`filesUnder` を通し、区切りをそろえてから比べる。

## 観点 3：作り直したのに、前の作りの「選ばせる場所」が残っていないか

> **2026-08-08。** 招待 URL を共有リンクに作り直したとき、
> 「無期限・何度でも・複数人」は直したのに、
> **発行する側が相手の役割を選ぶ**という前の作りだけ残していた。
> 依頼は「単純にリンクで送る」だったので、依頼者から作り直しを指摘された。
>
> テストは全件緑だった。**残していた場所そのものを固定するテストを
> 書いていた**ためである（「付与できる役割は Super User と
> Read Only だけ」）。消すべきものを守るテストは、消し忘れを隠す。
>
> 入口が 3 か所あり、そのうち曲の詳細だけ Read Only 固定になっていた。
> **入口ごとに結果が違う**のは、作りが 1 つに決まっていない印である。

- [ ] 作り直した機能に、**利用者が選ぶ場所**はいくつ残っているか
- [ ] その選択は、**選ぶ人がその答えを知っている**か
      （配る側は、相手が参加するかどうかも知らない）
- [ ] 同じ機能の入口が複数あるとき、**どれを使っても結果は同じ**か
- [ ] その選択肢を固定しているテストは、**残すべきものを守っているか**
      作り直しで消す予定のものを守っていないか

**やり方：** 依頼に「単純に」「そのまま」と書かれていたら、
実装後に**押すまでに何回選ばせているか**を数える。
0 回にできないか、まず疑う。

---

## 更新の仕方

**次の監査で「見つけられなかった欠陥」が出たら、ここに追記してください。**
見つけられた欠陥は追記しません。手順書がすでに拾えているためです。

| 追記した日 | きっかけ |
| --- | --- |
| 2026-08-06 | 第 1 回の監査で見つけられず、配信後に判明した欠陥（ホームに参加リストが出ない） |
| 2026-08-06 | 同上（項目の追加ができない。ルールのコメントと実装の食い違い） |
| 2026-08-06 | 統合テストが一度も通しで実行されておらず、前提が崩れたまま一部が PASS していた |
| 2026-08-06 | 第 2 回。「動かせない」と書いた本人が主宰者だった／常に赤いテストが抜け道になっていた／前回の指摘が記録だけで直っていなかった／前回の修正が新規登録を壊していた |
| 2026-08-07 | 2 回の監査がどちらも見落とし、配信後に判明（キャッシュ指定により、追加したアイコンが出ない） |
| 2026-08-07 | 足した部品が組み込まれないままビルドされ、ビルドも配信もテストも緑のまま機能だけが死んでいた |
| 2026-08-07 | 同日に 3 件、原因がいずれもアプリの外側にあった（配信の設定・クラウド側の権限・ビルドの生成物） |
| 2026-08-07 | 例外を握りつぶしていたため、原因が誰にも分からず配信のやり直しを繰り返した |
| 2026-08-07 | 第 3 回。片側（Dart）だけ見張りを置き、もう片側（TypeScript）で同じ死蔵が育っていた／「両方直すこと」という注意書きを仕組みと数えていた／対になる側（画面の言語）を見ずにメールの言語だけ直していた |
| 2026-08-08 | 共有リンクに作り直したのに、前の作りの「発行側が役割を選ぶ」だけ残していた。それを固定するテストがあったため、全件緑のまま気づけなかった |
