# 引き継ぎ（2026-08-08 時点）

**この文書は、担当を交代する人が最初に読むものです。**
いま何が終わっていて、次に何をすればよいかを、判断の理由つきで残します。

- 対象ブランチ：`claude/attachment-continuation-ryb7wv`
- **ブランチの内容は検証環境・本番環境の両方に配信済みです**（2026-08-08）
- 詳しい経緯：[DEVLOG.md](DEVLOG.md) — 同じ失敗を繰り返さないための記録
- **依頼者とのやりとりの原文**：[CONVERSATION-LOG.md](CONVERSATION-LOG.md)
  — 要約では落ちる言い回しを確かめたいときに
- **直近の監査**：[第 3 回（2026-08-07）](AUDIT-2026-08-07.md) — 未着手の積み残しもここに
- 仕様：[MusicListApp_Spec.md](MusicListApp_Spec.md)（v1.2 — 共有リンクは 3.3）
- 手順：[SETUP.md](SETUP.md)

---

## 0. 最初に読むべき決めごと

**[../CLAUDE.md](../CLAUDE.md) を必ず読んでください。**
依頼者から明示的に言われた、外してはいけない約束が書いてあります。

> **テストを飛ばして配信することは、依頼者が明示的にそう言わない限りありません。**
> 「テストは必須ではないので先に配信しても構いません」といった案内をしてはいけません。

順序は常に「全件実行 → 全件成功を確認 → 配信」です。
1 件でも赤い状態で「影響が無いので進められます」とこちらから促さないこと。
進めてよいかを決めるのは依頼者です。

そのほかの約束：

- 変更は必ずブランチ `claude/attachment-continuation-ryb7wv` に対して行う
- プルリクエストは、**依頼者が明示的に求めたときだけ**作る
- サービスアカウントの鍵は絶対にコミットしない
- `functions/.env*` はコミットするので、秘密の値を書かない
- エミュレータは必ず `--project demo-musiclist` で起動する

---

## 1. いま何が動いているか

| 環境 | プロジェクト ID | URL | 状態 |
| --- | --- | --- | --- |
| 検証 | `music-storage-dev` | <https://music-storage-dev.web.app> | 配信済み（2026-08-08 の版） |
| 本番 | `music-storage-d79b2` | <https://music-storage-d79b2.web.app> | 配信済み（2026-08-08 の版） |

**未配信の差分はありません。** ブランチの内容がそのまま両方の環境に入っています。
2026-08-08 に依頼者が本番まで配信し、「本番まで反映しました」と確認を受けています。

### テストの件数（2026-08-08 時点）

| 実行するもの | 件数 | エミュレータ |
| --- | --- | --- |
| `flutter analyze` / `flutter test` | 278 | 不要 |
| `cd rules-test && npm test` | 124 | スクリプトが自動で起動・終了 |
| `cd functions && npm test` | 75 | 不要 |
| `cd functions && npm run test:integration` | 61 | **別のウィンドウで `npm run serve` が必要** |

> **注意：作業用のコンテナ内では `rules-test` が緑になりません。**
> Storage のルールランタイムから Firestore を引く通信がプロキシに遮られ、
> 8 件がスキップされます。**コードの問題ではありません。**
> テスト自身がその旨を出力して止まるので、出力を読んでください。
> 依頼者の Windows 環境では 124 件すべて通ります。
> 統合テストも同じ理由でコンテナ内では実行できません。
> **この 2 つは依頼者に実行を依頼してください。**

---

## 2. これまでに入れた変更（すべて本番まで配信済み）

依頼者の求めた 9 項目のうち、2（メール招待）は**依頼者の指示で取りやめ**、残り 8 項目を実装しました。

| # | 依頼内容 | 実装 | コミット |
| --- | --- | --- | --- |
| 1 | ホームのリスト行から招待を選べるように | 行末の「︙」メニュー | `e31746f` |
| 2 | メールアドレスで招待 | **取りやめ**（「SMTP は不要です。メールで送信は削除して、URL をコピーだけに」） | — |
| 3 | コピーした URL から参加できるように | 既存の招待受諾フローに接続 | `e31746f` |
| 4 | リストを開いたとき左に再生ボタン | `_PlaybackButtons` を `ListTile.leading` へ | `6eed729` |
| 5 | 押したらそのまま再生 | Storage の URL を取り出して再生 | `6eed729` |
| 6 | 再生中は停止・一時停止をアイコン表示 | 同上 | `6eed729` |
| 7 | 停止は先頭へ、一時停止はその位置で | `PlaybackCommand.stop` / `resume` | `6eed729` |
| 8 | 登録メールを日本語でも送る | `setLanguageCode`。判断は**送信者の表示言語** | `6ce4bee` |
| 9 | 確認 URL を踏んだら画面側も自動で先へ | 3 秒ごとに静かに確認 | `6ce4bee` |
| — | 配信しても新しいアイコンが出ない不具合 | `firebase.json` のキャッシュ指定 | `7917450` |
| — | 音として鳴らせるファイルにだけ再生ボタンを出す | `isPlayableAudio`（画像に出て失敗していた） | `7917450` |
| — | 引き継ぎ文書の作成と、文書一式の実態合わせ | この文書ほか | `ed5a868` |
| — | `internal` の復旧手順を onCall 15 件に絞る | Cloud Shell 用の一括コマンド | `186e686` |
| — | 再生できない理由を「詳細」で読めるようにする | `play()` を待たない／URL を覚える | `ca48419` |
| — | 失敗の知らせを 15 秒に | 4 秒では押しそこねる | `b1377c1` |
| — | 知らせが 1 つも出ない欠陥の修正 | 画面で 1 か所だけ受ける | `154f0f8` |
| — | 足した部品が組み込まれないまま配信されるのを防ぐ | `deploy.mjs` が自動で `flutter clean` | `ec97064` |

### 1 番について依頼者に未確認の点

依頼は「**右側にパンくずリスト**を置き、クリックで選べるように」でした。
パンくずリスト（現在地を `A > B > C` と示すもの）は階層内の位置を示す部品で、
今回の「その行に対する操作を選ぶ」用途には合いません。
**行末の「︙」メニューとして実装し、その旨は依頼者に伝えてあります。**
「画面に出してみて意図と違うようならお知らせください」と伝えた状態で、
まだ返事をいただいていません。**次に画面を見てもらうときに確認してください。**

### 招待 URL を共有リンクに作り直しました（2026-08-07〜08）

**1・3 番の作りは、この改修で置き換わっています。** 上の表は経緯として
残しますが、いまの仕様は [仕様書 3.3](MusicListApp_Spec.md) を見てください。

依頼は「リストもしくは曲から**単純にリンクで**送る」「受け取った人が
**登録するか閲覧だけか**を決める」「リンク先にその旨を書く」
「**無期限**で**複数人が何度でも**使える」の 4 点でした。

| 変わった点 | いま |
| --- | --- |
| 期限・使用回数 | **無い。** 何人が何度使ってもよい |
| 止める手段 | **取り消しだけ。** 期限切れという歯止めが無くなったぶん重い |
| 受け取った人 | 「参加する」「参加せずに見る」から選ぶ。どちらもログインとメール確認は必要 |
| 閲覧だけの人 | `lists/{listId}/viewers/{uid}`。メンバー数にも通知の宛先にも入らない |
| すでにメンバー | **拒否しない。** 何度でも使えるリンクでは二度目が普通に起きる |

**そのあと、依頼者から作り直しの指摘を受けています。**

> 意図していたものと違います。「リンクをコピー」を１つだけにしてください。
> ユーザーの種類でわけないでください。

最初の実装では**発行する側が相手の役割を選ぶ**形（ホームのメニューが
2 項目、メンバー管理に選択ボタン、曲の詳細は Read Only 固定）でした。
古い招待の作りを引きずったもので、依頼の「単純にリンクで送る」に
反していました。**いまは押すだけの操作が 1 つあるだけです。**

- 発行時に指定するのは**対象リスト**（と、曲を指すならその曲）だけ

**そのあと、さらに指摘を受けています。**

> URLは単なるリスト、曲へのリンクでしかなく、それ以上の意味を
> 持たせてはいけません

選択肢を消した代わりに、`JOIN_ROLE = 'superUser'` をリンクの側に
書き込んでいました。**依頼者が消したかったのは「選ぶ操作」ではなく、
リンクが役割を持っていることでした。**

URL は転送されます。リンクに役割が書いてあると、**その URL を渡すこと
自体が書き込み権限を配ること**になり、渡した相手がさらに回せば
そのまま広がります。期限も回数の上限も無いリンクなので、なおさらです。

いまはこうなっています。

| 項目 | いま |
| --- | --- |
| `shareLinks/{linkId}` | `listId` と（あれば）`itemId` だけ。**役割の欄を持たない** |
| 参加したときの役割 | `domain/roles.ts` の `INITIAL_JOIN_ROLE`（**Read Only**） |
| 書けるようにする | リスト管理者がメンバー管理から上げる（5.4） |

一番低い役割で入れるのは、**リンクが与える範囲を「参加せずに見る」と
同じに揃えるため**です。URL が広く出回っても、それだけでは誰も
書けるようになりません。

**この 3 回で学んだことは 1 つです。** 選択肢を消すときは、
**その判断がどこへ行ったかを見ること。** 消えていないなら、
消したことにはなりません。

---

## 3. 直近の不具合と、その原因（重要）

### 配信したのに、追加したアイコンが出ない

**症状：** 再生ボタンを足して検証環境へ配信したが、**ボタンの場所だけ空いていて絵柄が出ない**。
強制リロード（Ctrl + Shift + R）でも直らない。検索の虫めがねなど**既存のアイコンは出る**。

**原因：`firebase.json` のキャッシュ指定。** アプリのコードではありません。

Flutter はアイコン用の `MaterialIcons-Regular.otf` を、**そのビルドで使っている
アイコンだけに削り込んで作り直します**（1.6MB → 13KB）。
つまり**名前は変わらないのに中身がビルドごとに変わる**ファイルです。

そこを `max-age=31536000, immutable`（1 年、取り直さない）にしていたため、

- `main.dart.js`（新しい）→ 再生ボタンを配置する
- `MaterialIcons-Regular.otf`（**古い**）→ 再生・一時停止・停止の絵柄を持たない

という組み合わせになりました。**場所だけ取って何も描かれない**という出方をします。

**直したこと：** `must-revalidate` に変更（`7917450`）。
`test/domain/hosting_cache_test.dart` で戻らないよう固定してあります。

**まだ残っている影響：** 一度でも開いたことがあるブラウザには、
古いフォントが「1 年取り直すな」の指定つきで残っています。**配信し直しても消せません。**
利用者に一度だけ **F12 → Application → Clear site data** をお願いする必要があります。
本番も同じ状態です（前回の本番配信は修正前の設定で行いました）。

**切り分け方：** シークレット ウィンドウで開いて直るなら、原因はキャッシュです。

**この件から学んだこと：**
「ファイル名が変わらない = 中身が変わらない」ではありません。
[AUDIT-CHECKLIST.md](AUDIT-CHECKLIST.md) の観点 6 に追記してあります。

### 本番でリスト作成の申請が `internal` で失敗する

**症状：** 本番環境でリスト作成を申請すると `internal` とだけ表示される。

**原因：Cloud Run の呼び出し許可が入っていません。** アプリのコードではありません。

`onCall` の関数は、Cloud Run の側で「誰でも呼べる」状態にしておく必要があります
（利用者の確認は関数の中で `request.auth` を見て行う設計です）。
Firebase CLI は関数を**新規作成したときだけ**この設定を入れます。

**2026-08-07 の初回本番配信は、20 件が「Failed to create」で失敗し、
再実行で「update」になって成功しました。** 関数はできたが、
**作成時にしか入らない呼び出し許可だけが入らなかった**状態です。
以後 `firebase deploy` を何度繰り返しても更新扱いなので直りません。

**直し方は [SETUP.md](SETUP.md) の「呼び出し可能関数が `internal` で失敗するとき」にあります。**
Cloud Shell から `gcloud functions add-invoker-policy-binding` を
**onCall の 15 件**に対して実行するのが早く、関数を止めずに済みます。
トリガーと定期実行は利用者が直接呼ばないので、対象外です。

**確かめ方：** Cloud Functions のログに次が出ていれば、この原因で確定です。

```
The request was not authenticated. ... Empty Authorization header value.
```

**出ていなければ別の原因です。** `internal` は関数の中で想定外の例外が出たときにも出ます。
ログの本文を読んでください。

`functions/test/setup_doc.test.ts` で、手順書の関数一覧が実装とずれないよう固定しました。
一覧から漏れた関数は、復旧手順を実行しても直らないためです。

### 再生ボタンを押しても鳴らない

**症状：** mp3 を登録して再生ボタンを押すと「再生できませんでした」。
検証環境・本番の両方で起きました。

**原因：`just_audio` の Web 版が組み込まれないままビルドされていた。**
アプリのコードの問題ではありません。

```
MissingPluginException(No implementation found for method init
on channel com.ryanheise.just_audio.methods)
```

Web 版が居ないため、Web には存在しないスマホ用の通信路を呼びに行き、
そこで止まっていました。`just_audio` は今回はじめて足した部品なので、
それ以前から残っている生成物が混ざると、**古い部品一覧がそのまま使われる**
ことがあります。`flutter clean` で直ります。

**この出方がいちばん危険です。**

- ビルドは成功する
- 配信も成功する
- 画面も普通に開く
- **その部品を使う操作だけ**が失敗する

どこにも赤が出ないまま機能だけが死にます。**自動テストでも捕まりません。**
テストは Dart のコードを直接動かすので、Web のビルドを通らないためです。

**手当て：** `scripts/deploy.mjs` が、部品の顔ぶれが前回のビルドから
変わっていたら自動で `flutter clean` します（`ec97064`）。
変わっていなければ何もしません（毎回消すとビルドが数分延びるため）。

### この 3 件に共通すること

**どれもアプリのコードの外側が原因でした。**

| 症状 | 原因のあった場所 |
| --- | --- |
| アイコンが出ない | 配信の設定（キャッシュ指定） |
| `internal` で失敗 | クラウド側の権限（Cloud Run の呼び出し許可） |
| 再生できない | ビルドの生成物（古い部品一覧） |

**手元で動くのに配信先で動かないときは、アプリの外側を疑う層に入れてください。**
`flutter test` はどれも緑のままでした。

### 原因を追えなくしていたのは、こちら側でした

「再生できませんでした」としか出ない作りにしていたため、
依頼者に何度も配信をやり直させました。例外を握りつぶしていたからです。

```dart
catch (_) { ...再生できませんでした... }   // ← 中身を捨てていた
```

**「失敗しました」とだけ出す作りは、直す機会そのものを奪います。**
いまは「詳細」から技術的な内容を読めます。同じ判断を他の画面でもしないこと。

---

## 4. 次にやること

**配信は終わっています。** 2026-08-08 に依頼者が本番まで反映しました。
急いで着手すべき作業は残っていません。ここには
「まだ確かめられていないこと」と「積み残し」を書きます。

### 4.1 配信はこの環境からできません

作業用コンテナには Firebase の認証情報がなく（`firebase login:list` が
`No authorized accounts`）、**配信はできません。** 依頼者の Windows から実行します。
曖昧にせず、そう伝えたうえで手順を渡してください。

配信の手順そのものは 4.4 に残してあります。

### 4.2 本番でまだ確認が取れていないこと

依頼者から「本番まで反映しました」とだけ伺っており、
**次の 3 点は、こちらでは確認できていません。** 次に画面を見てもらう
機会があれば、あわせて確かめてください。

| 確かめること | なぜ |
| --- | --- |
| 共有リンクを開くと「参加する／参加せずに見る」の画面が出る | URL が `#/s/…` の形なら新しい版。`#/invite/…` なら古い版が残っている |
| リンクから参加した人が **Read Only** で入っている | リンクが役割を持たないことの、最終的な確認（3.3） |
| 曲の ▶ で**実際に音が鳴る** | 2026-08-07 の `MissingPluginException` の再発が無いこと |

**とくに 1 つ目。** 新しく足した 3 本の関数
（`createShareLink` / `acceptShareLink` / `revokeShareLink`）は、
本番では**今回が初めての作成**でした。`onCall` の呼び出し許可
（Cloud Run の `allUsers`）は**新規作成のときにしか設定されません**。
過去にここが抜けて `！internal` になった実績があります（3 節）。
リンクを 1 本作って開いてみるのが、いちばん確実な確認です。

### 4.3 依頼者に未確認のまま残っている点

**1 番の「パンくずリスト」の見た目。**
依頼は「右側にパンくずリストを置き、クリックで選べるように」でした。
パンくずリスト（現在地を `A > B > C` と示すもの）は今回の用途に合わないため、
**行末のアイコンとして実装し、その旨は伝えてあります。**
「画面に出してみて意図と違うようならお知らせください」と伝えた状態で、
まだ返事をいただいていません。**画面を見てもらうときに確認してください。**

なお、この場所は 2026-08-08 に「︙」メニューから
🔗 のボタン 1 つへ変わっています（2 節）。

### 4.4 これから変更を入れるときの手順

**テストを飛ばして配信することはありません**（0 節）。
順序は常に「全件実行 → 全件成功を確認 → 配信」です。

```
cd C:\Users\1206441\MusicStore
git pull origin claude/attachment-continuation-ryb7wv

flutter analyze
flutter test                                   :: 278 件
cd functions && npm test                       :: 75 件
cd ..\rules-test && npm test                   :: 124 件
（1枚目）cd ..\functions && npm run serve
（2枚目）cd ..\functions && npm run test:integration   :: 61 件
```

全件成功を確認してから配信します。

```
scripts\deploy.cmd            :: 検証環境
scripts\deploy.cmd prod       :: 本番環境（プロジェクト ID の入力を求められる）
```

- **`prod` を付け忘れると検証環境へ流れます。**
- **`--no-build` を付けないでください。** 検証環境向けに作った `build/web`
  がそのまま本番へ載り、**本番の URL で検証環境につながる**状態になります。
  `deploy.cmd prod` は本番向けに作り直すので、そのまま実行してください
- 検証環境と本番のあいだで `git pull` からやり直す必要はありません。
  同じコミットを両方へ配信します

#### 配信の出力で見るところ

- 関数が `create` か `update` か。**新しい関数は `Successful create operation`**
  が出ているか（呼び出し許可は新規作成時にしか付かない）
- Hosting が最後まで通ったか。途中で失敗すると
  サイトが「Site Not Found」になります
- ローカルに無くなった関数の削除を聞かれたら、削除して構いません

### 4.5 積み残し

優先度つきの一覧は [BACKLOG.md](BACKLOG.md) にあります。特に上位のもの：

- **利用者向けマニュアル**（未作成。画面からは推測できない業務ルールが多い）
- **手動テストケース台帳**（仕様 12.6 が「別途整備する」としたまま）
- **項目編集時のファイル差し替え**（未実装。仕様書にも未実装と明記済み）

### 4.6 直せていない、既存利用者への影響

**2026-08-06 より前に本番を開いた方は、いまもアイコンが欠けて見えます。**

当時のキャッシュ指定が「1 年取り直すな」だったため、その方のブラウザには
古いアイコンフォントが残り続けます。**あとから配信しても消せません。**

- 初めて開く方：影響ありません
- 一度でも開いた方：**Clear site data**、またはシークレット ウィンドウ

指定自体は直っており（`7917450`）、以降に増やすアイコンでは起きません。
`test/domain/hosting_cache_test.dart` が戻りを見張っています。

---

## 5. この作業で分かった、繰り返しやすい落とし穴

次の担当が同じところで止まらないよう、実際に起きたものだけを挙げます。
詳細は [DEVLOG.md](DEVLOG.md) にあります。

### コンテナがリポジトリを巻き戻すことがある

このセッション中に **5 回**、作業ディレクトリが古いコミットへ
戻っていました。ファイルが消え、`git log` も巻き戻ります。

**push した直後でも起きます。** 2026-08-08 には、push を終えた次の
やりとりで `7917450`（2 日前）まで戻っており、**画面のファイルが
存在しないのに `flutter analyze` は緑**という状態になりました。
そのときは「作ったはずの画面が無い」と読んで実装漏れを疑いましたが、
実際には巻き戻りでした。**まず `git log --oneline -1` を見てください。**

**気づき方：** 直前に書いたはずのファイルが `No such file or directory` になる。
**戻し方：**

```sh
git fetch origin claude/attachment-continuation-ryb7wv
git checkout -B claude/attachment-continuation-ryb7wv origin/claude/attachment-continuation-ryb7wv
```

**push さえしてあれば失われません。** こまめに push してください。
戻したあとは、目印になる記号を `grep` して本当に戻ったか確かめること。

### 画面の見え方を、スクリーンショットの目測で診断しない

3 節の不具合を調べる際、スクリーンショット上の座標から
「配信済みか未配信か」を当てようとして往復しました。**当たりません。**

代わりに効いたのは次の 2 つで、どちらも機械的に確かめられます。

- ウィジェットを実際に描画して、アイコンの矩形・色・大きさを出力する使い捨てのテスト
- `flutter build web --release` を実行し、**生成されたフォントの cmap を直接読んで**
  必要な符号位置（`play_arrow` = U+E4CB 等）が入っているか数える

**「見えない」の原因は、コード・ビルド成果物・配信・キャッシュのどこにでもあります。**
層ごとに切り分けてください。

### 同じ制限が別の入口にもある

**2026-08-07 に、自分の申し送りを読んだ翌ターンにやりました。**

関数の中身を読む工程には 10 秒の制限があり、Node の版が違うと超えます。
エミュレータの起動でこれに当たり、`functions/serve.mjs` で待ち時間を
延ばしました。**配信（`scripts/deploy.mjs`）を忘れていました。**
数十分後、同じエラーで配信が止まりました。

| 入口 | 直した順 |
| --- | --- |
| `functions/serve.mjs`（エミュレータ） | 先に直した |
| `scripts/deploy.mjs`（配信） | **忘れた** |

**環境変数や設定を 1 か所に入れたら、同じものを渡している入口を
全部数えること。** このリポジトリで firebase を起動する場所は
`scripts/deploy.mjs`・`scripts/dev-emulators.mjs`・`functions/serve.mjs`・
`rules-test/run.mjs` の 4 つあります。

### 「配信済みのはず」と決めつけない

**このセッションで最も時間を無駄にした原因です。**

依頼者が「配信した」と言ったので、こちらは新しいビルドが載っている前提で
診断を進めました。実際には `git pull` が `firebase.json` の変更で止まっており、
**お手元は 5 コミット前のまま**でした。

気づけたのは、依頼者が
「**実際にでていないから。プライベートウインドで開きなおしてもでないから**」
と押し返してくれたからです。

**対策：どの版が載っているかを、言葉ではなく形で確かめる。**

```
git log --oneline -1        ← 期待するコミットが出るか
```

そのうえで「新しい版なら必ずこう見える」と言い切れる形にしておくと、
食い違いはその場で見つかります。今回は
「新しい版には**『詳細』の付かない通知を出す経路が存在しない**」と
言えたので、そこで確定しました。

### `git pull` が止まったときの判断

`Your local changes to the following files would be overwritten by merge`
で止まったら、**まず `git diff <ファイル名>` で中身を見ます。**

| ファイル | 扱い |
| --- | --- |
| `lib/env/firebase_options_staging.dart` | **残す**（捨てると `REPLACE_ME` に戻り接続できない） |
| `lib/env/firebase_options_prod.dart` | **残す**（同上） |
| それ以外（`firebase.json` など） | `git checkout -- <ファイル名>` で捨てる |

`firebase.json` が勝手に書き換わる事象は**2 回**起きています。
どちらも 100 行前後が 1 行に潰れていました。リポジトリ側が正本です。

### Windows 固有の注意

- 環境変数は `PATH` ではなく `Path`。大文字小文字を無視して既存のキーを探すこと
  （新しく `PATH` を作ると Windows からは別物になり、**PATH が消える**）
- `.cmd` は ASCII のみ。日本語を書くと文字化けして動かない
- `spawn(args[], {shell:true})` は引用符を付けずに連結する。Windows では 1 本の文字列で渡す
- `JAVA_TOOL_OPTIONS=` のような POSIX の書き方はコマンドプロンプトで動かない
- **`new URL(...).pathname` でファイルの場所を作らない。** Windows では
  `/C:/Users/...` と先頭にスラッシュが付いた形になり、`join` で繋ぐと
  `C:\C:\Users\...` になって開けない。`fileURLToPath` を使うこと
  （2026-08-07、監査で足したテストが Windows だけで落ちた）

**Windows 固有の不具合は、この環境では見つかりません。** ここは Linux
なので、書いた本人は緑を見て終わります。**Windows でしか動かない道
（`.cmd`・パス・環境変数）に触れたら、その旨を添えて依頼者に渡すこと。**

### エミュレータのプロジェクト ID

**必ず `--project demo-musiclist`。** `.firebaserc` の既定は検証環境の
`music-storage-dev` なので、付け忘れると**実在のプロジェクト ID で立ち上がり**、
統合テストと噛み合いません。しかもそのとき、
「〜が起きないこと」を確かめる項目が**通知が 1 件も作られないために PASS**します。
土台が崩れているほど緑が増えるという出方をします（監査 第 2 回）。

### 進捗はこまめに報告する

依頼者から「進行状況を逐次表示してください」「都度教えてください」と
**5 回以上**求められました。長い作業の途中でも、区切りごとに現状を出してください。

---

## 6. コードの地図（今回さわった範囲）

| ファイル | 役割 |
| --- | --- |
| `lib/domain/playback.dart` | 再生の状態遷移と、再生できるファイルかの判定。**音を鳴らさずにテストできる** |
| `lib/data/audio_player_handle.dart` | 音を鳴らす部分。テストで差し替えるための境界 |
| `lib/providers/playback_provider.dart` | 画面と上記をつなぐ。`downloadUrlResolverProvider` は**テストのために切り出した** |
| `lib/ui/screens/list_detail_screen.dart` | `_PlaybackButtons`（一覧の左の再生操作） |
| `lib/ui/screens/home_screen.dart` | `_ShareLinkMenu`（行末の 🔗 から共有リンクをコピー。**選択肢は無い**） |
| `functions/src/domain/share_link.ts` | リンクの判断。落ちる理由は「見つからない」「取り消し済み」の 2 つだけ |
| `functions/src/domain/roles.ts` | `INITIAL_JOIN_ROLE` — 参加した人の初期役割（Read Only）。**リンクは役割を持たない** |
| `lib/ui/screens/share_link_screen.dart` | リンクを開いた人が「参加する／参加せずに見る」を選ぶ画面 |
| `lib/data/repositories/auth_repository.dart` | `_applyLanguage`（確認メールの言語） |
| `lib/ui/screens/auth/verify_email_screen.dart` | 3 秒ごとの静かな確認（自動で先へ進む） |
| `firebase.json` | 配信するファイルのキャッシュ指定。**3 節の不具合の原因** |
| `test/domain/hosting_cache_test.dart` | 上記が戻らないよう固定 |
| `scripts/deploy.mjs` | 配信。**部品の顔ぶれが変わったら自動で `flutter clean`** |
| `functions/src/errors.ts` | エラーを符号で返す。画面が文言を引く（多言語化のため） |
| `functions/test/setup_doc.test.ts` | 手順書の関数一覧が実装とずれないよう固定 |

### 設計上、崩してはいけない点

- **`lib/domain/` は Firebase に依存させない。** 通信なしでテストできることが前提です
- **再生の判断は `domain/playback.dart` に置く。** 画面に直接書くと、
  「テストは緑だが本番では別のコードが動く」状態になります（監査で 2 回続けて出た指摘）。
  `test/domain/no_dead_code_test.dart` が機械的に見張っています
- **画面に出る文字は必ず `lib/l10n/*.arb` を通す。** 直書きすると
  英語表示のときだけ日本語が出ます

---

## 7. 依頼者とのやりとりで、守ったほうがよいこと

- **秘密の値をこちらへ貼らせない。** 過去に SMTP の資格情報の話が出た際、
  「この値は私に見せないでください」と伝えて Secret Manager へ誘導しました
- **一度 OK と言われたことを聞き直さない。**
  「1 は OK と言いました。しつこく聞かないように」と指摘を受けています
- **できないことは、できないと理由つきで言う。**
  配信はこの環境からはできません。曖昧にせず、代わりに何をすればよいかを示すこと
- **自分の誤りは、そう書く。** 前回の本番配信では、Hosting が途中で失敗したのに
  「関数だけ再実行すればよい」と案内し、サイトが「Site Not Found」になりました。
  スクリプトの完了メッセージは修正済みです（`9951fa8`）
- **失敗を「失敗しました」で終わらせない。** 再生できない件では、例外を
  握りつぶしていたために原因が誰にも分からず、依頼者に何度も配信を
  やり直させました。**原因を出す作りにしておけば 1 往復で済みます**
- **押し返されたら、まず自分の前提を疑う。** 「配信済みのはず」という
  こちらの思い込みが、遠回りの直接の原因でした（5 節）
- **同じ指摘を 2 回受けたら、言葉どおりに読み直す。**
  共有リンクでは、同じ依頼を 3 回、言い方を変えて言われました

  | 回 | 依頼者の言葉 | こちらの受け取り方 |
  | --- | --- | --- |
  | 1 | 「ユーザーの種類でわけないでください」 | 発行画面の選択肢を消した |
  | 2 | 「URLを受け取った人自身が選択する」 | 画面はもうあります、と答えた |
  | 3 | 「URLは単なるリンクでしかない」 | **リンクから役割を取り除いた** |

  1 回目で「選択肢を消す」までしか読まず、消した判断を
  リンクの側へ移して済ませていました。**選択肢を消すときは、
  その判断がどこへ行ったかを見てください。** 消えていないなら、
  消したことにはなりません
- **「実装済みです」と答える前に、実物を見せる。** 2 回目のとき、
  画面が既にあることを文章で説明しましたが伝わりませんでした。
  実際に描画した画像を出したところ、依頼者の論点が
  **画面ではなくリンクの中身**にあることが分かりました。
  見た目の確認は、`flutter test --update-goldens` で
  画面を PNG に書き出せば手早く作れます（使い捨てにしてください）

---

## 8. 第 3 回のゼロベース監査（2026-08-07）

記録は [AUDIT-2026-08-07.md](AUDIT-2026-08-07.md)。**7 件を直しました。**

| 度合い | 内容 | 対処 |
| --- | --- | --- |
| 高 | 新しく登録した人の表示言語が必ず日本語になる | 3 つの入口すべてで、使っている言語を残すようにした |
| 中 | メール確認の自動問い合わせに止めどきが無い | 10 分で止める。Auth の回数制限に当たる恐れがあった |
| 中 | 呼び出し口に日本語が直書きされていた | 消して、画面が l10n から引くようにした |
| 中 | サーバー側に死蔵コード 3 件（**3 回連続**） | 削除し、TypeScript 側にも見張りを置いた |
| 中 | サーバーの符号一覧をテストが写していた | その場で読むようにした |
| 低 | 使われていない文言 6 件 | 削除し、増えないよう見張りを置いた |
| 低 | 招待 URL と日時の表示にテストが 0 件 | テストを足した（招待 URL は壊れると誰も参加できない） |

**次に読む人へ、特に伝えたいこと。**

- **片側だけ塞ぐと、もう片側で同じことが起きます。** 第 2 回で Dart 側に
  死蔵の見張りを置き、TypeScript 側には置きませんでした。結果、
  TypeScript 側で同じ欠陥が育ちました
- **「〜すること」という注意書きは仕組みではありません。** 機械的に
  確かめられるものは、コメントではなくテストにしてください
- **直したときは、対になる側を見てください。** 確認メールの言語を直した
  その日に、画面の言語は固定のままでした

**3 回連続で残っている積み残しが 2 つあります。**
利用者向けマニュアルと、手動テストケース台帳です。どちらも
「何をどこまで書くか」が依頼者の判断になるため、監査の中では
決めていません。**残り続けていること自体を見落とさないでください。**
